#MonkeyTime CONFIG - prod

server:
  port: 443
  ssl:
    enabled: true
    key-alias: tomcat
    key-store: "classpath:certificate.p12"
    key-store-type: PKCS12
    key-store-password: ${MonkeyTimePass}

spring:
  application:
    name: MonkeyTime